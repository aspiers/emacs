# -*- mode: snippet -*-
# name: inst
# key: inst
# --
#!/bin/sh

set -e
R=${1:-FILENAME_BASE}
upak-wget $R.tar.gz
gunzip -qc $R.tar.gz | tar xvf -
cd $R
./configure --prefix=$UPAKINSTALLDIR
make
make install
