All init files have an `as-' prefix.  This clarifies that they are
opinionated, designed only for my usage, and typically not instantly
reusable.  It also makes it safer to put this directory on
`load-path', which will aid mapping of dependencies between these
files.
